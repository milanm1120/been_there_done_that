<a href= "/pins/new">Create Pin</a> || 
<a href= "/pins"> Pins</a> || 
<a href= "/destinations/new">Create Destination</a> || 
<a href= "/destinations"> Destinations</a> 


  <% if !logged_in? %>          <!-- helper method defined in helpers/application_helper.rb --> 
      <%= button_to "Sign In With Google", '/auth/google_oauth2' %>
  <% else %>
      <%= link_to "Log Out", logout_path, method: 'delete' %>
  <% end %>